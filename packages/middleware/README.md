# @salmon/middleware

Shared Express middlewares: `logger`, `cacheControl` (Cache-Control headers per route), and `sanitize` (input sanitization).

Internal package — consumed only by this repo. See the root `AGENTS.md` for what belongs in `packages/`.
