# @salmon/middleware

Shared Express middlewares: `logger` and `cacheControl` (Cache-Control headers per route).

Internal package — consumed only by this repo. See the root `AGENTS.md` for what belongs in `packages/`.
