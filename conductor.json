{
  "scripts": {
    "setup": "test -n \"$CONDUCTOR_ROOT_PATH\" || { echo \"Missing CONDUCTOR_ROOT_PATH. Run this from Conductor.\" >&2; exit 1; }; test -n \"$CONDUCTOR_WORKSPACE_PATH\" || { echo \"Missing CONDUCTOR_WORKSPACE_PATH. Run this from Conductor.\" >&2; exit 1; }; test -n \"$CONDUCTOR_PORT\" || { echo \"Missing CONDUCTOR_PORT. Run this from Conductor.\" >&2; exit 1; }; export ENV_SOURCE=\"$CONDUCTOR_ROOT_PATH/.env\"; export ENV_TARGET=\"$CONDUCTOR_WORKSPACE_PATH/.env\"; test -f \"$ENV_SOURCE\" || { echo \"Missing $ENV_SOURCE. Create a gitignored .env at the repository root.\" >&2; exit 1; }; if [ \"$ENV_SOURCE\" != \"$ENV_TARGET\" ] && ! cmp -s \"$ENV_SOURCE\" \"$ENV_TARGET\" 2>/dev/null; then cp \"$ENV_SOURCE\" \"$ENV_TARGET\"; fi",
    "run": "test -n \"$CONDUCTOR_ROOT_PATH\" || { echo \"Missing CONDUCTOR_ROOT_PATH. Run this from Conductor.\" >&2; exit 1; }; test -n \"$CONDUCTOR_WORKSPACE_PATH\" || { echo \"Missing CONDUCTOR_WORKSPACE_PATH. Run this from Conductor.\" >&2; exit 1; }; test -n \"$CONDUCTOR_PORT\" || { echo \"Missing CONDUCTOR_PORT. Run this from Conductor.\" >&2; exit 1; }; export ENV_SOURCE=\"$CONDUCTOR_ROOT_PATH/.env\"; export ENV_TARGET=\"$CONDUCTOR_WORKSPACE_PATH/.env\"; test -f \"$ENV_SOURCE\" || { echo \"Missing $ENV_SOURCE. Create a gitignored .env at the repository root.\" >&2; exit 1; }; if [ \"$ENV_SOURCE\" != \"$ENV_TARGET\" ] && ! cmp -s \"$ENV_SOURCE\" \"$ENV_TARGET\" 2>/dev/null; then cp \"$ENV_SOURCE\" \"$ENV_TARGET\"; fi; cd \"$CONDUCTOR_WORKSPACE_PATH\"; export COMPOSE_PROJECT_NAME=\"salmon-api-conductor\"; export OFFLINE_HTTP_PORT=\"$CONDUCTOR_PORT\"; export OFFLINE_LAMBDA_PORT=$((CONDUCTOR_PORT + 1)); export CONDUCTOR_REDIS_PORT=$((CONDUCTOR_PORT + 2)); docker compose up --build",
    "archive": "test -n \"$CONDUCTOR_ROOT_PATH\" || { echo \"Missing CONDUCTOR_ROOT_PATH. Run this from Conductor.\" >&2; exit 1; }; test -n \"$CONDUCTOR_WORKSPACE_PATH\" || { echo \"Missing CONDUCTOR_WORKSPACE_PATH. Run this from Conductor.\" >&2; exit 1; }; test -n \"$CONDUCTOR_PORT\" || { echo \"Missing CONDUCTOR_PORT. Run this from Conductor.\" >&2; exit 1; }; export ENV_SOURCE=\"$CONDUCTOR_ROOT_PATH/.env\"; export ENV_TARGET=\"$CONDUCTOR_WORKSPACE_PATH/.env\"; test -f \"$ENV_SOURCE\" || { echo \"Missing $ENV_SOURCE. Create a gitignored .env at the repository root.\" >&2; exit 1; }; if [ \"$ENV_SOURCE\" != \"$ENV_TARGET\" ] && ! cmp -s \"$ENV_SOURCE\" \"$ENV_TARGET\" 2>/dev/null; then cp \"$ENV_SOURCE\" \"$ENV_TARGET\"; fi; cd \"$CONDUCTOR_WORKSPACE_PATH\"; export COMPOSE_PROJECT_NAME=\"salmon-api-conductor\"; export OFFLINE_HTTP_PORT=\"$CONDUCTOR_PORT\"; export OFFLINE_LAMBDA_PORT=$((CONDUCTOR_PORT + 1)); export CONDUCTOR_REDIS_PORT=$((CONDUCTOR_PORT + 2)); docker compose down --remove-orphans"
  }
}
